repositories{    
 // jcenter()
  flatDir {
      dirs 'libs'
   }
}

dependencies {
   compile(name:'printactivity-release-2.1.5', ext:'aar')
   compile(name:'core-3.3.0.jar', ext:'jar')
}

android {
  packagingOptions {
    exclude 'META-INF/NOTICE'
    exclude 'META-INF/LICENSE'
  }
}